#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <unistd.h>
#include <fcntl.h>

void	ft_cat(int);
char	*ft_strcpy(char *, const char *);
char	*ft_strncpy(char *, const char *, size_t);
void	*ft_memcpy(char *, const char *, size_t);
char *	ft_strchr(const char *, int);
char *	ft_strnchr(const char *, int, size_t);

int		main(void)
{
	return (0);
}
